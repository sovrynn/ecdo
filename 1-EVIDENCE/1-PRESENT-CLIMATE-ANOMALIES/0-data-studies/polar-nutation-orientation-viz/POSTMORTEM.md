So the stuff in here is just visualizing the TPW.

The stuff in the `geomagnetic-wander-overlay` is overlaying TPW with NPW in a single graph.

# The data

So the original data is in arcseconds

I believe